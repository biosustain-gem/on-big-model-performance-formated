// import { fromJS } from 'immutable';
// import { makeSelectDataProviderDomain } from '../selectors';

// const selector = makeSelectDataProviderDomain();

describe('makeSelectDataProviderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
