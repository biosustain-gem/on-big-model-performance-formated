// import { fromJS } from 'immutable';
// import { makeSelectScrollerDomain } from '../selectors';

// const selector = makeSelectScrollerDomain();

describe('makeSelectScrollerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
